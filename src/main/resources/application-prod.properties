# ===============================
# MongoDB Configuration - Spring Boot
# ===============================

# Use the environment variable MONGO_URL if set (Railway deployment)
# Otherwise fallback to Railway public URL (for local development)
spring.data.mongodb.uri=mongodb://mongo:EExbLkLzTuIBYYNsbxMJaqbpiqCDSvmE@mongodb.railway.internal:27017

# Database name (optional, as URI already specifies it)
spring.data.mongodb.database=mongo
#122
# Credentials (optional, URI already has username & password)
spring.data.mongodb.username=mongo
spring.data.mongodb.password=EExbLkLzTuIBYYNsbxMJaqbpiqCDSvmE

# Host & port (optional, URI already has host & port)
spring.data.mongodb.host=mongodb.railway.internal
spring.data.mongodb.port=27017
